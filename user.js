// create a user class
export default class User {
    constructor(name, tableNumber) {
        this.name = name;
        this.tableNumber = tableNumber;
    }

    // set table - choose a table randomly and check if it is occupied
    // if occupied, choose another table
    // if not occupied, occupy the table
    // if no table is available, display error message
}